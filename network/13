echo "dpm-master.42.us.org."

# basically get the domain name from IP address.
# command : dig +short -x 10.51.1.81 
# dig (domain information groper)
# network administration command-line tool for querying DNS servers
# -x : for reverse lookups
# +short : get short form of answer
